  <nav class="navbar is-danger">
      <div class="navbar-brand">
      <%= link_to root_path, class: "navbar-item" do %>
        <h1 class="title is-5">CoursesLearn</h1>
      <% end %>
      <div class="navbar-burger burger" data-target="navbarExample">
          <span></span>
          <span></span>
          <span></span>
      </div>
      </div>

      <div id="navbarExample" class="navbar-menu">
        <div class="navbar-end">
          <div class="navbar-item">
          <div class="field is-grouped">
            <% if user_signed_in? %>

              <div class="field has-addons">
              <div class="control">
                   <%= form_tag courses_path, :method => "get" do %>
              <%= text_field_tag :title, params[:title], placeholder: "Search", class: "button is-danger is-inverted" %> 
              <%= submit_tag("Search", :name => nil, :class=>"button is-danger") %>
              <%end %>
             </div>
            </div>
                <p class="control">
                  <%= link_to new_course_path, class: "button is-danger" do %>
                  <span class="icon"><i class="fas fa-plus-square"></i></span>
                  <% end %>
                </p>
                <p class="control">
                  <%= link_to current_user.email, edit_user_registration_path, class: "button is-danger is-inverted" %>
                </p>
                <p>
                  <%= link_to "Logout", destroy_user_session_path, method: :delete, class:"button is-danger" %>
                </p>
              <% else %>
                <p class="control">
                  <%= link_to 'Sign In', new_user_session_path, class: "button is-danger" %>
                </p>
                <p class="control">
                  <%= link_to 'Sign Up', new_user_registration_path, class: "button is-danger" %>
                </p>
              <% end %>  
            </div>
          </div>
        </div>
      </div>
    </nav> 